FASTAPI_HOST=127.0.0.1
FASTAPI_PORT=8000

TUTOR_MODEL=gemini-2.5-flash-native-audio-preview-09-2025

# Gemini API keys are no longer read from here - each profile stores its
# own key (set it in the app's sidebar, under "Gemini API key"). Get a
# free key from https://aistudio.google.com/apikey
